interface:
  display_name: "Agent Plugin Tester"
  short_description: "Run deterministic audio plug-in tests"
  default_prompt: "Use $use-agent-plugin-tester to validate this VST3 or macOS AUv2 plug-in with the GUI and a JSON report."
